# spring-standard-fileupload

Spring Boot application using servlet standard file upload components.

## Build & Run

``` bash
./mvnw clean spring-boot:run
```

Open a browser and access `http://localhost:8080`.

